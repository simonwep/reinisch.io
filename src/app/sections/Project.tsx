import {FunctionalComponent, h} from 'preact';
import styles from './Projects.module.scss';

export const Project: FunctionalComponent = () => {
    return (
        <div className={styles.project}>

        </div>
    );
};
